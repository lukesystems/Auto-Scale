/** Columns safe for enrich/classify when optional migrations are missing. */
export const TRENDWATCH_SOURCE_ENRICH_SELECT =
  "id, source_url, platform, account_handle, account_type, published_at, follower_count, views, likes, saves, shares, comments, transferability_score, notes";
